#P5E9
#Escribe un programa que pida la anchura y la altura de un rectángulo y lo dibuje de la siguiente manera
###ESTE NO LO ENTIENDO~###
altura=int(input("Dame la altura de un rectangulo "))
anchura=int(input("Dame la anchura de un rectangulo "))
for i in range(altura):
    if (i==0) or (i==altura -1):
        print ("{}" .format("*"*(anchura)))
    else:
        print ("*{}*" .format(" "*(anchura -2)))
